import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.clinicalhandbook.app',
  appName: 'Clinical Handbook',
  webDir: 'out',
};

export default config;
